[project]
name = "webapp"
version = "0.1.0"
description = "Python gaps for AI: virtual environments and packages with uv - class demo code"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.2",
]
